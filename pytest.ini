[pytest]
testpaths = tests
markers =
    unit: fast unit tests for local logic
    integration: multi-module tests covering artifact generation and serialization
    functional: end-to-end or smoke checks for user-facing workflows and assets
